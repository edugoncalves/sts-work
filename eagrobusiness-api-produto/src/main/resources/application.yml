server:  
  port: ${PORT:7000}

spring:  
  application:
    name: produto-api



eureka:
      instance:
        prefer-ip-address: true
      client:
        register-with-eureka: true
        fetch-registry: true
        service-url:
          defaultZone: http://${HOST_EUREKA:localhost}:8761/eureka/
  