server:
  port: 8001
spring:
  application:
    name: eureka-client
    
eureka:
  client:
    serviceUrl:
      defaultZone: http://${EUREKA_DISCOVERY_SERVER:localhost}:8761/eureka/
  instance:
    preferIpAddress: true

ribbon:
  eureka:
    enabled: true