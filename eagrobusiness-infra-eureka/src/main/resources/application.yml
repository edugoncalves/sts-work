server:
  port: 8761
  
spring:
  application:
      name: eureka-server
eureka:
  instance:
    hostname: discovery-server
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${HOST_EUREKA:localhost}:${server.port}/eureka/